# Shrek-Castle-Run-

A fun 2D browser-based platformer game where a friendly green ogre sets out to rescue a princess trapped in a dragon’s castle. Built using HTML5, CSS, and JavaScript/TypeScript, the game features colorful cartoon art, simple controls, and lighthearted fairy-tale humor.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies: `npm install`
2. (Optional) Set the `POmi_API_KEY` in `.env.local` if you use external APIs
3. Run the app: `npm run dev`
